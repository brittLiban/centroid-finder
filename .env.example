# Server port (frontend expects 3001)
PORT=3001

# Path to the compiled Java JAR (relative to server/ folder)
JAR_PATH=../processor/target/centroidfinder-1.0-SNAPSHOT-jar-with-dependencies.jar

# Video input directory (relative to server/ folder)
VIDEO_INPUT_DIR=../videos

# Results output directory (relative to server/ folder)
OUTPUT_DIR=../results
