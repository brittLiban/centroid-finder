version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    volumes:
      - ./hostVideos:/videos
      - ./hostResults:/results
    environment:
      - PORT=3001
      - VIDEO_INPUT_DIR=/videos
      - OUTPUT_DIR=/results
